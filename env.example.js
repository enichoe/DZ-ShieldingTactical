// RENOMBRAR A `env.js` y rellenar con tus valores (NO subir a git)
// Ejemplo de archivo para exponer variables en runtime al frontend.

// window.SUPABASE_URL = "https://your-project.supabase.co";
// window.SUPABASE_ANON_KEY = "public-anon-key";

/*
Instrucciones:
1. Copia este archivo como `env.js` en la raíz del proyecto.
2. Reemplaza los valores por los tuyos.
3. Asegúrate de que `env.js` se carga antes de `js/supabaseClient.js`.
*/
